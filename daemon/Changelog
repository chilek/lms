* 11.01.2004
  - w 'payments' poprawiony b³±d w obliczaniu yearstart (alec)
  - w 'payments' dodana obs³uga pola 'paytype' z tabeli invoices (alec)

* 10.01.2004
  - przebudowa 'payments', tzn. poprawione bugi, kosmetyka i dodana
    obs³uga op³at sta³ych (alec)
  - almsd nie korzysta ju¿ z tabeli 'reload' (lexx)
  - usuniête `` w zapytaniach w almsd.c (alec)
      
* 09.12.2003
  - 'dhcp' i 'dns' przystosowane do nowego formatu adresu sieci (alec)

* 04.12.2003
  - dodane funkcje db_pexec() i db_pquery() (alec)
  - w zapytaniach u¿ywamy teraz '%NOW%' zamiast '?NOW?' (alec)
 
* 02.12.2003
  - modu³ 'payments' przystosowany do nowych faktur (alec)

* 01.12.2003
  - w 'payments' poprawione obliczanie %period oraz dodana zmienna %tariff,
    która zostaje zamieniona nazw± taryfy (alec)
  - do 'payments' dodana opcja 'up_payments'. ustawiaj±c j± na 'yes' (lub 1) 
    okres (%period) bêdzie obliczany "z góry" (alec)    
    
* 27.11.2003
  - w 'ethers' dodana mo¿liwo¶æ wyboru (opcja dummy_macs), czy dla 
    komputerów od³±czonych ustawiaæ mac 00:00:00:00:00:00 (alec)
  - ujednolicone opcje begin i end, zamiast prefix, start, append (alec)
  - w 'ggnotify' zmiana opcji passwd na bardziej intuicyjn± password (alec)
  - w modu³ach notify ujednolicono opcje template (alec)

* 26.11.2003
  - dodany modu³ 'ggnotify' (lexx/alec)
  - poprawiona obs³uga warto¶ci NULL w mysql (alec)
  - 'ethers' zapisuje mac-adresy 00:00:00:00:00:00 dla hostów
    z access=0 (alec)
  - w 'cutoff' limit podajemy ze znakiem [tak jak w 'notify'] (alec)

* 20.11.2003
  - dodany modu³ 'tc', czyli htb (alec)
  - dodany modu³ 'notify' (alec)

* 18.11.2003
  - dodany modu³ 'traffic' odpowiednik perlowego lms-traffic (alec)

* 12.11.2003
  - du¿a liczba poprawek, g³ównie usuniêcie memory leaks (alec)

* 10.11.2003
  - dodany modu³ 'cutoff' (alec)
  - dodany modu³ 'payments' (alec)
  - dodanie do GLOBAL brakuj±cej db_exec() (alec)

* 07.11.2003
  - przebudowana funkcja str_replace() i co siê z tym wi±¿e za³atana
    powa¿na dziura, teraz funkcja zwraca ilo¶æ wyst±pieñ zamienianego
    ci±gu, co mo¿na bêdzie te¿ gdzie¶ wykorzystaæ (alec)
  - poprawione b³êdy w module dns powoduj±ce wysypanie programu gdy w 
    danych s± dwa dns'y oraz niezapisywanie do dhcpd.conf wiêcej ni¿
    jednej podsieci (alec/lexx)

* 05.11.2003
  - dodany modu³ 'dns' (alec)
  - za³atane dziury w db_free() i iniparser_load() (alec)

* 03.11.2003
  - zmiana w obs³udzie instancji w lms.ini nie ma ju¿ load0, load1, args0 itd.
    teraz w sekcji [lmsd] mamy 'instances = lista instancji', a argumenty 
    i modu³y w osobnych sekcjach. Zobacz lms.ini.sample (alec)
  - inicjalizacja modu³ów podczas reloadu, a nie jak by³o dot±d podczas
    startu daemona (alec)
  - dodany modu³ 'oident' (alec)
  - wyrzucony modu³ 'firewall', którego zastêpuje 'hostfile' (alec)
  - ujednolicone zmienne (tmpfile -> file) (alec)
  - du¿o zmian kosmetycznych, na przyk³ad zmiana funkcji save_string na 
    str_save (alec)
  - dodatkowa zmienna 'info' w instancjach, przygotowana m.in. pod przysz³e
    zmiany w sposobie reloadu (alec) 
  - dodane zwalnianie zasobów w modu³ach (alec)
    
* 01.11.2003
  - mo¿na korzystaæ z instancji (alec)

* 31.10.2003
  - poprawiony b³±d z pustymi warto¶ciami zmiennych konfiguracyjnych (alec)
  - zrobiony nowy ./configure (alec)
  - dwa poziomy debugowania:
	debug0 - logowanie zapytañ SQL
	debug1 - logowanie zdarzeñ (alec)
  - zmiana 'tempfile' na 'tmpfile' w konfiguracji modu³ów (alec)

* 29.10.2003
  - dodane parsowanie sekwencji specjalnych w zmiennych konfiguracyjnych (alec)

* 28.10.2003 
  - dodane modu³y firewall, dhcp, hostfile, ethers (alec)

* 26.10.2003
  - pierwsze kroki (alec)
CFLAGS+=-I../lib -D_GNU_SOURCE
OBJECTS+=../../../dbdrivers/$(DBDRIVER)/db.o ../../../util.o \
	tscript_exec.o sql.o
HEADERS=tscript_exec.h sql.h 

extensions.a: $(OBJECTS)
	ar rs tscript_extensions.a $(OBJECTS)

$(OBJECTS): %.o: %.c $(HEADERS)
	gcc -c $< $(CFLAGS)

clean:
	rm -f tscript_extensions.a *.o
